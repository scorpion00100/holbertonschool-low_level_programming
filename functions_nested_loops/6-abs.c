#include "main.h"

/**
 * _abs - compute absolute valute of an int
 * @n: integr num
 * Return: value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

	return (0);
}
